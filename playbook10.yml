---
- name: Implementing global scope variables
  hosts: all
  tasks:
    - name: Install/Uninstall of s/w apps
      apt:
        name: "{{a}}"
        state: "{{b}}"
        update_cache: "{{c}}"
...
